type TholidayProps = {
  counties?: String;
  countryCode: String;
  date: String;
  fixed: boolean;
  global: boolean;
  launchYear?: string;
  localName: string;
  name: String;
  types: string[];
};

type TindexProps = number;

type TpropsCardDays = {
  PublicHolidays: [];
};
